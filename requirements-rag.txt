# ── Optional RAG research pipeline ──────────────────────────────────────────
# NOT installed by default (see requirements.txt). Install only if you want
# RAG_RESEARCH_ENABLED=true. This pulls in torch (CPU build), which needs
# roughly 2 GB of disk and adds real RAM overhead at runtime — think twice
# before enabling this on a 1 GB Oracle "Micro" shape.
#
# Install with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-rag.txt
#
# (torch is installed separately from its own CPU-only index above; if you
# let this file install it too, remove the --index-url line, since pip
# doesn't read custom index URLs from inside a requirements file
# automatically unless declared with --extra-index-url. Simplest and most
# reliable is still the two-command sequence above.)

sentence-transformers
faiss-cpu
langchain-text-splitters
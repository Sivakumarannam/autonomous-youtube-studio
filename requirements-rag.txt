# Optional RAG stack — install only when the VM has enough RAM/disk.
# Do NOT add these to the main requirements.txt on Oracle Always Free (~1 GB).
#
#   pip install -r requirements-rag.txt
#
# Enables: FAISS vector store + sentence-transformers embeddings for chatbot RAG.
# Keep RAG_RESEARCH_ENABLED=false on the 1 GB production VM.

faiss-cpu>=1.7.4,<2
sentence-transformers>=2.2.0,<4

# torch CPU wheel (avoids pulling CUDA / nvidia-* packages):
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<3
